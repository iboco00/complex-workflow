name: Complex

on: push

jobs:
    ubuntu:
        name: First Job
        runs-on: ubuntu-latest
        steps:
            - run: data
    windows:
        name: Second Job
        runs-on: windows-latest
        steps:
            - run: data
    macos:
        needs: [job1, job2]
        name: Third Job
        runs-on: macos-latest
        steps:
            - run: data
     depends:  
      needs: ubuntu,windows,macos
      runs-on: macos-latest
      steps:
        -run:date
        
            
